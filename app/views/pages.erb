<div class="container">
  <h1>Pages</h1>
  <ul>
    <% @pages.each do |page| %>
        <li>
          <a href="/page/<%= page.id %>/edit"><%= page.title %> (<%= page.path %>)</a>
        </li>
    <% end %>
  </ul>
</div>
